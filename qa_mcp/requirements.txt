mcp>=1.2.0,<2.0.0
httpx>=0.27.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
PyJWT>=2.9.0
python-docx>=1.1.2
python-dotenv>=1.0.1
msal>=1.31.0
pytest>=8.3.0
uvicorn[standard]>=0.30.0
# azure-identity:
# Official Azure SDK package to interact with Azure Identity services.
# Used for authenticating and obtaining tokens for Azure resources.
azure-identity>=1.0.0,<2.0.0

# azure-keyvault-secrets:
# Official Azure SDK package to interact with Azure Key Vault.
# Used to securely store and retrieve sensitive information like API keys and secrets.
azure-keyvault-secrets>=4.0.0,<5.0.0

# -----------------------------------------------------------------------------
# TELEMETRY AND LOGGING
# -----------------------------------------------------------------------------

# opencensus-ext-azure:
# Integration of OpenCensus with Azure Monitor.
# Allows tracing and telemetry data (like spans, metrics) to be sent to Azure Application Insights.
opencensus-ext-azure>=1.0.0,<2.0.0

# fastapi:
# A modern, fast (high-performance), web framework for building APIs with Python 3.7+.
# Used as the main API engine in this application.
fastapi[standard]>=0.135.0,<0.136.0

# aiohttp:
# An asynchronous HTTP client for Python, used for making HTTP requests.
aiohttp>=3.13.0,<3.14.0